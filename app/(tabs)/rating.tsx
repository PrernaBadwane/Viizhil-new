import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import RatingPage from '@/components/Src/RatingPage'

const rating = () => {
  return (
    <View style={{flex:1}}>
    <RatingPage />
   </View>
  )
}

export default rating

const styles = StyleSheet.create({})