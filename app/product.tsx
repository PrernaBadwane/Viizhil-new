import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Addproductlist from '@/components/Src/Addproductlist'

const product = () => {
  return (
    <View style={{flex:1}}>
     <Addproductlist  />
    </View>
  )
}

export default product

const styles = StyleSheet.create({})